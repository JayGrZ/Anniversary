/** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

const nextConfig = {
    reactStrictMode: true,
    images : {
      domains : ['i.pinimg.com', 'localhost', 'assets.aceternity.com'] // <== Domain name
    }
  }
  
  export default nextConfig